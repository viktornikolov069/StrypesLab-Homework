#include <stdio.h>
#include "set_64.h"


int main() {
    uint64_t setA = 251;
    uint64_t setB = 217;
    // printf("\nresult -> %d\n", isSubset(setA, setB));
    // printf("\nDifference is %ld", Difference(setA, setB));
    // printf("")
    printf("\n251 - 217 = %ld\n", Difference(setA, setB));


    // for (uint8_t i = 0; i < 64; ++i) {

    // }
    
} 